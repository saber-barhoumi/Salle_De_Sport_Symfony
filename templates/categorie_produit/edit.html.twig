
{% extends 'base.html.twig' %}

{% block title %}Modifier la catégorie produit{% endblock %}

{% block body %}
    <h1>Modifier la catégorie de produit</h1>

    {# Formulaire d'édition de la catégorie produit #}
    {{ form_start(form) }}
        {{ form_row(form.nom) }}  {# Champ pour le nom de la catégorie #}
        <button type="submit" class="btn btn-primary">
            ✏️ Modifier
        </button>
    {{ form_end(form) }}

    {# Lien pour revenir à la liste des catégories de produits #}
    <a href="{{ path('categorie_produit_index') }}" class="btn btn-secondary">Retour à la liste</a>

    {# Formulaire de suppression avec emoji de poubelle #}
    <form method="post" action="{{ path('categorie_produit_delete', {'id': categorie_produit.id}) }}" 
          onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cette catégorie ?');" class="d-inline">
        <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ categorie_produit.id) }}">
        <button type="submit" class="btn btn-danger">
            🗑️ Supprimer
        </button>
    </form>
{% endblock %}
