{% extends 'base.html.twig' %}

{% block title %}Produit{% endblock %}

{% block body %}
    <h1>Produit</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ produit.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ produit.nom }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ produit.Description }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ produit.image }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ produit.prix }}</td>
            </tr>
            <tr>
                <th>QuantiteStock</th>
                <td>{{ produit.quantiteStock }}</td>
            </tr>
            <tr>
                <th>Catagorie</th>
                <td>{{ produit.CategorieProduit.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('produit_index') }}">back to list</a>

    <a href="{{ path('produit_edit', {'categorieId': produit.categorieProduit.id, 'id': produit.id}) }}">Éditer le produit</a>

    {{ include('produit/_delete_form.html.twig') }}
{% endblock %}
