{% extends 'base.html.twig' %}

{% block title %}Ajouter un Tag{% endblock %}

{% block body %}
    <h1>Ajouter un nouveau Tag</h1>

    {{ form_start(form) }}
        {{ form_row(form.nom) }}
        <button class="btn btn-primary">Enregistrer</button>
    {{ form_end(form) }}

    <a href="{{ path('tag_list') }}" class="btn btn-secondary mt-3">Retour à la liste des tags</a>
{% endblock %}